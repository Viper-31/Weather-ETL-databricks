# Weather-ETL-databricks

Testing out databricks connection to DPIRD weather station data. 

## Init on connect to serverless compute
1. Restart Python session
    - Ensure 'Base environment' is Python 3.12+
2. Install from requirements
```bash
pip install -r requirements.txt
```  
On right sidebar -> environment -> Drag & drop 'requirements.txt' 
